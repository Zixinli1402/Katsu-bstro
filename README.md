# Katsu Bistro website

A refreshed, responsive menu experience for Katsu Bistro. This is a lightweight static site with no build step: open `index.html` in a browser or deploy the repository to GitHub Pages.

## Run locally

```bash
python3 -m http.server 8000
```

Then visit `http://localhost:8000`.
